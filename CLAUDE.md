# CLAUDE.md

## Shell command rules (keep auto mode prompt-free)

This project runs Claude Code in auto mode with reads outside the working directory blocked. Any shell command that can't be statically analyzed triggers a manual approval prompt and stops the work. Follow these rules for every Bash call.

### Never pipe code into an interpreter
- Do NOT use `node -e`, `node -`, `python -c`, `python -`, `bash -c`, `sh -c`, `eval`, or `npx tsx -e`.
- Do NOT pipe into interpreters: `... | node`, `... | python`, `... | bash`.
- Do NOT use heredocs or here-strings in shell commands: `<<EOF`, `<<'EOF'`, `<<<`.
- Do NOT create or edit files via the shell (`cat > file <<EOF`, `echo ... > file`, `sed -i`). Use the Edit/Write tools instead.

### One-off scripts go in a file
1. Write the script with the Write tool to `.claude-tmp/<name>.mjs` (or `.ts` / `.py`). The folder is gitignored.
2. Run it with a plain command: `node .claude-tmp/<name>.mjs`.
3. Delete it when finished unless it is worth keeping. If so, move it to `scripts/` and add a package.json script.

### Keep commands simple
- Run from the repo root. Avoid `cd other/dir && ...` — every script below works from the root.
- One command per Bash call. Avoid long `&&` / `;` chains, subshells `$(...)`, and backticks.
- Keep everything inside the repo. Do not read or write `~`, `/tmp`, or other absolute paths outside the project. Use `.claude-tmp/` for scratch files and logs. Exception: the output file Claude Code itself names for a background command may be read — or run the command in the foreground instead.
- Prefer the package.json scripts (run with `pnpm`, never `npm`: the repo pins `pnpm@10.33.0` and uses `workspace:` / `catalog:` dependencies npm cannot install). Check `package.json` for the exact script names before running anything.

## Commands (repo root)

| What | Command |
|---|---|
| Typecheck every package | `pnpm typecheck` |
| Unit tests (vitest, every package under `packages/`) | `pnpm test:unit` |
| Unit tests of one package | `pnpm --filter @wirework/engine test` |
| E2E suite | `pnpm test:e2e` (alias `pnpm e2e`) |
| Everything CI runs | `pnpm check` |
| Playground dev server | `pnpm dev` (port 5173, strict — fails if one is already running; the developer often has it open) |
| Storybook | `pnpm storybook` (its play tests are not run by any script yet) |

`pnpm test` runs unit tests, then e2e.

## E2E tests (Playwright + playwright-bdd)

- Scenarios are Gherkin: `e2e/features/*.feature`, steps in `e2e/steps/steps.ts`. The `.spec.js` files in `e2e/.features-gen/` are GENERATED by `bddgen` — never edit or run them directly; `npx playwright test` alone skips `bddgen` and runs stale specs.
- Full suite: `pnpm test:e2e --reporter=line`
- Some feature files (name filter): `pnpm test:e2e refresher layout --reporter=line`
- One scenario: `pnpm test:e2e -g "<scenario title>" --reporter=line`
- The suite starts its own Vite server on port 5174 and never reuses one: do not run two e2e runs at the same time.
- Never use interactive flags: `--ui`, `--debug`, `--headed`, or `npx playwright show-report`. They block the session. Reports are written to `e2e/reports/` (cucumber HTML, Playwright HTML) for people to open.
- When a test fails, read the error output, the `.feature` file and `steps.ts` with the Read tool. Do not write ad-hoc scripts to inspect results.
- After changing test steps, rerun only the affected feature files first, then the full suite.
- The playground is a routed app (`apps/playground/src/routes.ts`): pages `overview` (/demo), `runs` (/demo/runs), `run` (/demo/runs/:runId), `settings` (/demo/settings) and `builder` (/builder). `I open the "<page>" page` loads the page's address (a fresh app); `I switch to the "<page>" page` clicks the menu (no reload); `I open the address "<path>"` is for addresses outside the menu.
- Scenarios assert fixture facts: adding a cell to the overview page changes "the page has N cells" in `layout.feature`, `overlay.feature` and `app.feature`; the fake runs server moves unfinished runs one step after every answer of the LIST (and starts over when the runs page opens), so a scenario's expected statuses depend on how many requests it made.
- The runs list is a table the SERVER describes (`packages/table-view`): columns and the filter bar come from the first answer of the fake `/api/v1/view/runs`, rows are flat (`state`, not `status.state`), and the page's changes (Inbound hidden, a "Suite" filter of its own, the `#` and Name cells as links to `/demo/runs/{id}`, Status as a tag per state) are declared in the table's own `load` reaction (`widgets.runs.table` in the fixtures). `I click the table row` clicks the row's corner, never a link; `I click the link "<text>" in the table row "<key>"` follows one (`table-cells.feature`).
- Load events (`doc/widget-events-design.md`): every page emits `page · load` once per opening and every table `antd-table · load` once when it appears, so the event log is never empty — a scenario counting log entries counts those too (overview: 1 before any click; runs: 2). After **Add widget** and after **Save page** the page LOADS AGAIN (both events fire again, tables re-request): in the builder every Add adds a `page · load` entry (plus one per table), and on the runs page a Save costs one more list request, which moves the fake server's time one step.
- Every page visit starts that page's OWN data over (`apps/playground/src/boot.ts`): the runs table is empty until the fake server answers (~600 ms). The demo app's GLOBAL state (`app`, `viewModels`, `userViewModels`) stays while you move between its pages; the user, permissions and lists arrive with a mock session request (~300 ms); it starts over after a visit to the builder or a reload. The builder's store has NO data: a builder scenario that needs data seeds it itself with the doc-string step `the store holds at "<path>":`.
- The user overlay starts over with the visit too: ON for the demo, OFF for the builder, where it is unavailable until the page has a widget. With it on, the builder's Add is locked (widgets are added to the shared page).

## Where things are

- `packages/schema` contracts, model types, path rules · `store` · `events` (bus) · `engine` (registries, resolve, validation, reactions) · `react` (adapter) · `engine-*` (layout engine plugins) · `widget-contracts` (standard kinds) · `antd-widgets` · `table-view` (actions for the view table API: a table the server describes) · `view-data-models-examples` (fixtures)
- `apps/playground` the demo host (boot in `src/boot.ts`, routes in `src/routes.ts` and `src/router.tsx`) · `e2e` the Gherkin suite
- `doc/*.md` the engine's design decisions (widget IO, events and reactions, contracts, actions, layout engines) with status tables; `doc/guidelines.md` coding guidelines; `doc/tableApi/` the view table API's example requests and responses (read by the table-view unit tests); `doc/team_tiger.md` the review personas

## Commits

One-line messages. No `Co-Authored-By` trailer.
